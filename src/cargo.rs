/*

cargo new
    --bin : binary
    --lib : library

cargo build ("cargo b")
    --release

cargo run ("cargo r")
cargo test ("cargo t")
cargo clean

cargo --version


Configuration :
- On peut configurer le linter rust dans VSC : paramètre "Rust-analyzer › Check: Command"
    "test" pour le linter classique (valeur par défaut)
    "clippy" pour le linter amélioré (vraiment pas mal)

*/